{
  "name": "playwright-testkube-demo",
  "version": "1.0.0",
  "description": "Sample Playwright suite for running in Testkube",
  "scripts": {
    "test": "playwright test"
  },
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
